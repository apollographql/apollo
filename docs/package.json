{
  "scripts": {
    "start": "rm -rf .cache && gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve"
  },
  "dependencies": {
    "gatsby": "^2.4.2",
    "gatsby-theme-apollo-docs": "^1.0.0-alpha.54"
  }
}
