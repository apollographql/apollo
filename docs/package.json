{
  "scripts": {
    "prestart": "gatsby clean",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "netlify": "gatsby build --prefix-paths",
    "mv": "mkdir -p docs && mv public/* docs && mv docs public/ && mv public/docs/_redirects public",
    "noindex": "echo 'User-agent: *\nDisallow: /' > public/robots.txt"
  },
  "dependencies": {
    "gatsby": "2.27.0",
    "gatsby-theme-apollo-docs": "4.7.2",
    "react": "16.13.1",
    "react-dom": "16.12.0"
  }
}
